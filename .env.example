# Vaulto basket-ETF backend.
# Defaults to the production Railway deployment if unset.
BASKET_ETF_API_URL=https://vaulto-api-etf-production.up.railway.app

# API key for the basket-ETF backend (x-api-key header).
# Required for ALL commands except `tokens` and `describe`.
BASKET_ETF_API_TOKEN=
